{
 "version": "2.0.0",
 "tasks": [
  {
   "type": "cppbuild",
   "label": "gcc build",
   "command": "C:/msys64/mingw64/bin/gcc.exe",
   "args": [
    "${file}",
    "-o",
    "${fileDirname}/${fileBasenameNoExtension}.exe"
   ],
   "options": {
    "cwd": "${fileDirname}"
   },
   "problemMatcher": ["$gcc"],
   "group": {
    "kind": "build",
    "isDefault": true
   }
  }
 ]
}